import Accordion from "./Accordion";

export default {
    title: 'Accordion',
    component: Accordion,
};

export const AccordionElement = () => {
    return <Accordion title={'Accordion Title'}/>
};
